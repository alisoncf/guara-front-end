export interface ObjetoFisico {
  obj: string; // URI completa do objeto
  id: string; // ID do objeto (final da URI)
  titulo: string;
  tipo: string;
  resumo: string;
  id_recortado: string;
  tipo_recortado: string;

}
